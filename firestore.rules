rules_version = '2';

// Personal site: content is world-readable; only the signed-in admin can write.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}

